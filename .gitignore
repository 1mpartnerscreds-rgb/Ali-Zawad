node_modules
.next
out
.vercel
.env
.env*.local
next-env.d.ts
*.tsbuildinfo
.DS_Store

# Sales operations — scraped prospect contact data and call scripts.
# This repo is public; the call list holds real businesses' phone numbers,
# and the scripts are competitive material. Kept locally, never pushed.
sprint/
.env*
